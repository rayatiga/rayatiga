/*
 AngularJS v1.8.2
 (c) 2010-2020 Google LLC. http://angularjs.org
 License: MIT
*/

// ZERO CODE
// ANGULAR LOADED FROM EXTERNAL SCRIPT!
// CHECK BACK-END

if (window.matchMedia && window.matchMedia("(prefers-color-scheme: dark)").matches) {
    // var colorScheme = document.getElementsByTagName('html')[0]
    // colorScheme.setAttribute('data-root-theme', 'dark')
    document.getElementsByClassName("navbar-brand")[0].classList.add("text-white")
    document.querySelectorAll(".bg-light").forEach((bgLight) => {
        bgLight.className = bgLight.className.replace(/-light/g, "-dark")
        bgLight.classList.add("text-white")
    })
    document.querySelectorAll(".nav-link").forEach((navLink) => {
        navLink.classList.add("text-light")
    })
    document.querySelectorAll(".dropdown-item").forEach((dropdownItem) => {
        dropdownItem.classList.add("text-white")
    })
    document.querySelectorAll(".small").forEach((small) => {
        small.classList.remove("text-muted")
        small.classList.add("text-light")
    })
    document.querySelectorAll(".link-dark").forEach((linkDark) => {
        linkDark.className = linkDark.className.replace(/-dark/g, "-light")
    })
    document.querySelectorAll(".btn-dark").forEach((btnDark) => {
        // btnDark.className = btnDark.className.replace(/-dark/g, "-light")
        btnDark.classList.add("border-secondary")
    })
    document.querySelectorAll(".feature").forEach((feature) => {
        feature.classList.add("border", "border-secondary")
    })
    document.querySelectorAll(".border-dark").forEach((borderDark) => {
        borderDark.classList.remove('border-dark')
        borderDark.classList.add('border-secondary')
    })
    document.querySelectorAll(".text-dark").forEach((textDark) => {
        textDark.classList.remove('text-dark')
        textDark.classList.add('text-light')
    })
    document.querySelectorAll(".border").forEach((border) => {
        border.classList.remove('border-dark')
        border.classList.add('border-secondary')
    })
    document.querySelectorAll(".card").forEach((card) => {
        card.classList.add('border-secondary')
    })
    document.querySelectorAll(".table").forEach((tableHover) => {
        tableHover.classList.remove('table-hover')
    })
    document.querySelectorAll(".disabled").forEach((tableHover) => {
        tableHover.classList.add('text-secondary')
    })
    document.getElementsByClassName("border-bottom")[0].classList.add("border-secondary")
    document.getElementsByClassName("border-top")[0].classList.add("border-secondary")
    document.querySelectorAll(".background-home").forEach((backgroundHome) => {
        backgroundHome.style.backgroundImage = "linear-gradient(rgba(50, 50, 50, 0.5), rgba(50, 50, 50, 0.5)), url(/asset/image/hosting/home.webp)"
        backgroundHome.style.borderBottom = '1px solid rgb(var(--root-secondary-rgb))'
    })
    document.querySelectorAll(".background-about").forEach((backgroundAbout) => {
        backgroundAbout.style.backgroundImage = "linear-gradient(rgba(50, 50, 50, 0.5), rgba(50, 50, 50, 0.5)), url(/asset/image/hosting/about.webp)"
        backgroundAbout.style.borderBottom = '1px solid rgb(var(--root-secondary-rgb))'
    })
    document.querySelectorAll(".background-contact").forEach((backgroundContact) => {
        backgroundContact.style.backgroundImage = "linear-gradient(rgba(50, 50, 50, 0.5), rgba(50, 50, 50, 0.5)), url(/asset/image/hosting/contact.webp)"
        backgroundContact.style.borderBottom = '1px solid rgb(var(--root-secondary-rgb))'
    })
    document.querySelectorAll(".background-pricing").forEach((backgroundPricing) => {
        backgroundPricing.style.backgroundImage = "linear-gradient(rgba(50, 50, 50, 0.5), rgba(50, 50, 50, 0.5)), url(/asset/image/hosting/pricing.webp)"
        backgroundPricing.style.borderBottom = '1px solid rgb(var(--root-secondary-rgb))'
    })
}
